// utils.js - fungsi utilitas
function strip(h){ const d=document.createElement('div');d.innerHTML=h;return(d.textContent||'').trim(); }
function getImg(h){ const m=h&&h.match(/<img[^>]+src=["']([^"']+)["']/i);return m?m[1]:null; }
function ago(ds){ if(!ds)return'—'; const s=(Date.now()-new Date(ds).getTime())/1000; if(s<60)return`${~~s}d lalu`; if(s<3600)return`${~~(s/60)}m lalu`; if(s<86400)return`${~~(s/3600)}j lalu`; return`${~~(s/86400)}hr lalu`; }
function toast(msg,type='info',dur=4000){ const z=document.getElementById('toast-zone'); const t=document.createElement('div'); t.className=`toast ${type}`; t.textContent=msg; z.appendChild(t); setTimeout(()=>{ t.classList.add('out'); setTimeout(()=>t.remove(),350); },dur); }
function setFeedStatus(text,color){ document.getElementById('fs-text').textContent=text; document.getElementById('fs-dot').style.background=color; const n={'var(--green)':5,'var(--yellow)':3,'var(--red)':1}[color]||0; [0,1,2,3,4].forEach(i=>{ document.getElementById(`fsb${i}`).style.background=i<n?color:'var(--surface3)'; }); }
function enhanceAccessibility(){ const zoomIn=document.querySelector('.leaflet-control-zoom-in'); const zoomOut=document.querySelector('.leaflet-control-zoom-out'); if(zoomIn){ zoomIn.setAttribute('aria-label','Perbesar peta'); zoomIn.setAttribute('title','Perbesar peta'); } if(zoomOut){ zoomOut.setAttribute('aria-label','Perkecil peta'); zoomOut.setAttribute('title','Perkecil peta'); } document.querySelectorAll('.sb-item[onclick], .fm-row[onclick], .fm-fly[onclick], #wire-handle, #wire-drag-bar').forEach(el=>{ if(!el.hasAttribute('role')) el.setAttribute('role','button'); if(!el.hasAttribute('tabindex')) el.setAttribute('tabindex','0'); if(!el.hasAttribute('aria-label')) el.setAttribute('aria-label', el.textContent.replace(/\s+/g,' ').trim() || 'Tombol navigasi'); }); document.querySelectorAll('[role="button"][tabindex="0"]').forEach(el=>{ if(el.dataset.keyReady)return; el.dataset.keyReady='1'; el.addEventListener('keydown',e=>{ if(e.key==='Enter'||e.key===' '){ e.preventDefault(); el.click(); } }); }); }
function setLoading(v, text='Memuat berita terbaru...'){ const loading=document.getElementById('loading'); const loadingText=document.getElementById('loading-text'); const wireList=document.getElementById('wire-list'); if(loadingText) loadingText.textContent=text; if(loading) loading.classList.toggle('hidden',!v); if(wireList) wireList.setAttribute('aria-busy', v?'true':'false'); }
const $ = id => document.getElementById(id);